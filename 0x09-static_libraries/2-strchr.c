#include "main.h"

/**
 * _strchr - function that locates a character in a string
 * @s: The string
 * @c: The character
 * Return: s or NULL if c not found
 */
char *_strchr(char *s, char c)
{
	return (s);
}
