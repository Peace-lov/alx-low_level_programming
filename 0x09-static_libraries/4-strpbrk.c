#include "main.h"

/**
 * _strpbrk - function that searches a string for any of a set of bytes
 * @s: the string
 * @accept: string that receives byte
 * Return: *s
 */
char *_strpbrk(char *s, char *accept)
{
	return (s);
}
