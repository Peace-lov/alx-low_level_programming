#include "main.h"
/**
 * _memset - sets memory
 * @s: the string
 * @b: the character
 * @n: fix number of memory
 * Return: character pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
